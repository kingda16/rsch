function [udot] = WIODE(delta,epsilon,u,DX,dx)



n = length(guess);
u = [zeros(1,n+2); zeros(n,1) guess zeros(n,1); zeros(1,n+2)];

ux = (DX*u')';
uy = (DX*u)';
uxx = (DX*ux')';
uyy = (DX*uy)';
uyx = (DX*uy')';
uxxx = (DX*uxx')';
uyyy = (DX*uyy)';
uxxxx = (DX*uxxx')';
uyyyy = (DX*uyyy)';

total = delta*u*2+4*(uxx.^2+uyy.^2-1).*(uxx+uyy)+2*epsilon^2*(ux.*(uxxxx+uyyyy)+;
total(:,1) = total(:,1)/2;
total(:,end) = total(:,end)/2;
total(1,:) = total(1,:)/2;
total(end,:) = total(end,:)/2;


end

